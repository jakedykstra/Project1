// Trade history database

// tdb if i need these params within (dependant on where the info needed are stored)
function tradeHistory(transaction, coinAmount, usdAmount) {
    // need to have database for all the firebase transactions

}

// Splash page unless session storage
function checkUser() {
    // check for session storage
    if(localStorage.getItem()){
        newUser === false;
        // if modal - $('.modal').style('display', 'name')
        // if changing to different path - location.replace('./index.html')  
    }